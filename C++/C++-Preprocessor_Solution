///////////////////////////////////////logic///////////////////////////////////////////////////

#define toStr(str) #str
#define io(v) cin>>v
#define FUNCTION(name,operatorr)
#define INF 10000000000
#define foreach(v,i) for(int i=0;i<v.size();i++)
#define maximum(a,b) a=max(a,b)
#define minimum(a,b) a=min(a,b)

///////////////////////////////////////end/////////////////////////////////////////////////////
